/**
 * Static educational copy for temperament results and HTML export (no user input).
 * Single source of truth for polarity framing (on-screen + export stay in sync).
 */

/**
 * Section 2 Option A: causal chain + intensity, then Section 1 Option A: second line = one-lens caveat.
 * Para 1 — Polarity → tension → attraction → role clarity → stability; matched intensity.
 * Para 2 — Not total reality; single structural lens.
 */
export const TEMPERAMENT_REPORT_TIER1_PARAS = [
  'Polarity is complementary placement on shared dimensions: that contrast holds tension; tension pulls attraction into focus; clear roles follow; the pair stabilizes when the pattern holds. The lever that makes it legible is a complementary opposite at matched intensity—not a half-step mismatch.',
  'Attachment, values, stress, and health still shape most outcomes; this report is one structural lens on placement and partner fit.'
];

/** Shown inside the primer card (and export) as one italic line—population dots are reference only. */
export const TEMPERAMENT_REPORT_SPECTRUM_NOTE =
  'The trend dots for males and females are population references, not targets. Couple fit is about complementary placement and calibrated strength of opposite poles—not both partners matching the same dot.';

/** Explains gender-aware scoring for the Selection Criteria dimension (no per-row jargon). */
export const TEMPERAMENT_REPORT_SELECTION_CRITERIA_NOTE =
  'The Selection Criteria dimension uses gender-aware weighting on some items so partner-preference answers stay on a comparable masculine–feminine signaling frame across male and female respondents; other dimensions are scored the same way as always.';

/** `<details>` summary line */
export const TEMPERAMENT_REPORT_TIER2_SUMMARY =
  'Shifts over time: exposure, biology, hormones';

/**
 * Section 3 Option A: biology integrated with polarity (not a separate “second model”).
 */
export const TEMPERAMENT_REPORT_TIER2_PARAS = [
  'When polarity stays complementary and a partner’s presence lines up with that pattern, biological reinforcement of how expression shows up over time is plausible—not guaranteed. Matched intensity matters partly because a weak or missing complement dulls the same dynamic this map is tracking.',
  'Scores are a snapshot; expression shifts with stress, beliefs, trauma patterns, and life phase. Mindset and stress also leave expressed temperament and endocrine “spark” feeling out of sync—worth noticing if you want both structure and chemistry.'
];
